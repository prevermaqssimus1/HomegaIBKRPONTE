package com.example.homegaibkrponte.model;

/**
 * Enum para representar a direção de uma posição de trading.
 */
public enum PositionDirection {
    LONG,  // Posição comprada (lucra com a alta do preço)
    SHORT  // Posição vendida (lucra com a baixa do preço)
}